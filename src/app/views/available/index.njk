{% for hour in available %}
  <label class = "{% if hour.available !== true %} disabled {% endif %}"for="time-{{hour.time}}">
    {% if hour.available === true %}
      <input type="radio" id="time-{{hour.time}}">
    {% endif %}
    {{ hour.time }}
  </label>
{% endfor %}
